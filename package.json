{
  "name": "@pratap/dns-server",
  "description": "Build your own DNS server challenge, from CodeCrafters",
  "type": "module",
  "scripts": {
    "dev": "bun run app/app.ts",
    "build": "bun build app/app.ts --outdir dist --target bun",
    "start": "bun run dist/app.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
